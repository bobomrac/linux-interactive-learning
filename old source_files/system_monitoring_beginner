#!/bin/bash
    clear
    echo "System Monitoring - Beginner level selected."
    echo "This level will guide you through basic system monitoring."
    echo ""
    echo "Step 1: First let's check the current system uptime. Use the 'uptime' command."
    echo ""
    read -p "Enter command: "
    eval "uptime"
    echo ""
    echo "Step 2: Now let's check the current system load. Use the command 'top'."
    echo "The top command displays the processes running on your system and their resource usage in real-time. You can use the 'q' key to exit top."
    echo ""
    read -p "Enter command: "
    eval "top"
    echo ""
    echo "Step 3: Let's see the memory usage on the system. Use the command 'free -m'."
    echo "The free command shows the total amount of free and used memory in the system, as well as the shared memory and buffers used by the kernel. The '-m' option shows the output in MB."
    echo ""
    read -p "Enter command: "
    eval "free -m"
    echo ""
    echo "Step 4: Now let's check the disk usage on the system. Use the command 'df -h'."
    echo "The df command shows the filesystem disk space usage for all mounted partitions. The '-h' option makes the output more human-readable."
    echo ""
    read -p "Enter command: "
    eval "df -h"
    echo ""
    echo "Step 5: Let's check the system's network connections. Use the command 'netstat -tuln'"
    echo "The netstat command shows various network-related information. The '-t' option shows the TCP connections, '-u' shows the UDP connections, '-l' shows only listening sockets, and '-n' shows numerical IP addresses instead of hostnames."
    echo ""
    read -p "Enter command: "
    eval "netstat -tuln"
    echo ""
    echo "CONGRATULATIONS! This was everything for the beginner level of System Monitoring. See you in the next chapter!"
    echo ""
    read -p "Press enter to go back to the main menu."

